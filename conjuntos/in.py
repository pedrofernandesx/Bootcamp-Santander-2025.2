#{}.in                        —> serve para verificar se o elemento está dentro do conjunto				

numeros = {1, 2, 3, 1, 2, 4, 5, 5, 6, 7, 8, 9, 0}

print(1 in numeros) # >>> true
print(10 in numeros) # >>> false
